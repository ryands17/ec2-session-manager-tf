{
  "name": "ec2-tf",
  "version": "1.0.0",
  "main": "main.js",
  "types": "main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "get": "yarn cdk get",
    "build": "yarn get && tsc",
    "synth": "yarn cdk synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok",
    "cdk": "cdktf",
    "upgrade": "yarn add cdktf@latest cdktf-cli@latest",
    "upgrade:next": "yarn add cdktf@next cdktf-cli@next"
  },
  "engines": {
    "node": ">=10.12"
  },
  "dependencies": {
    "@cdktf/provider-aws": "^0.0.43",
    "cdktf": "0.0.17",
    "constructs": "^3.0.4"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "cdktf-cli": "0.0.17",
    "dotenv": "^8.2.0",
    "husky": ">=4",
    "lint-staged": ">=10",
    "prettier": "^2.1.2",
    "typescript": "^4.0.3"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "arrowParens": "avoid"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,ts,json,md}": "prettier --write"
  }
}
